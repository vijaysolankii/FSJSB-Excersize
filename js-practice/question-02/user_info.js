// Declare variables to store your first name, last name, marital status, country and age and display them using interploation method

var userObj = {
    first_name : "Vijay",
    last_name : "Solanki",
    merital_status : true,
    country : "India",
    age : 24
}

console.log("User First name is : " + userObj["first_name"] + "\nUser Last name is : " + userObj["last_name"] + "\nUser Merital status is : " + userObj["merital_status"] + "\nUser Country is : " + userObj["country"] + "\nUser age is : " + userObj["age"])