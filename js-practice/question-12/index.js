// Create a human readable time format using the Date time object

let age_of_user = prompt("enter your age");
if (age_of_user > 18) {
  console.log("You are old enough to drive");
} else {
  console.log("wait for the number of years he needs to turn 18");
}
